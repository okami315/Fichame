<header>
    {% if app.user %}
        <div class="mx-2 my-1 text-right">
            <a class="text-dark" href="{{path("app_user_profile")}}"><i class="fas fa-user"></i> <b>{{ app.user.userIdentifier }}({{ app.user.dni }})</b></a>  - <a class="text-dark" href="{{path('app_company_show', {'nif':app.user.company.nif})}}">{{app.user.company.title}}({{app.user.company.nif}})</a> <a class="btn btn-danger" href="{{ path('app_logout') }}">Cerrar sesion</a>
        </div>
    {% endif %}
</header>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <ul class="navbar-nav container-fluid me-auto justify-content-around">
    {% if is_granted("ROLE_USER") %}
        {# <li class="nav-item"><a class="nav-link active" href="{{path("app_main")}}"><i class="fas fa-tasks"></i> Tareas</a></li> #}
        <li class="nav-item"><a class="nav-link active" href="{{path("app_event_index")}}"><i class="fas fa-bullhorn"></i> Eventos</a></li>
        {% if is_granted('ROLE_ALMACEN') %}
        <li class="nav-item"><a class="nav-link active" href="{{path("app_warehouse")}}"> <i class="fas fa-warehouse"></i> Almacén</a></li>
        {% endif %}

    {% endif %}
        
        {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item"><a class="nav-link active" href="{{path("app_user_index")}}"> <i class="fas fa-users-cog"></i> Usuarios</a></li>

            {% if is_granted('ROLE_SUPER_ADMIN') %}
                <li class="nav-item"><a class="nav-link active" href="{{path("app_company_index")}}"><i class="fa-solid fa-building"></i> Empresas</a></li>
            {% endif %}

            <li class="nav-item"><a class="nav-link active" href="{{path("app_user_trabajadores")}}"><i class="fas fa-users"></i> Admin</a></li>
        {% endif %}
        
    </ul>
</nav> 