<h1 class="text-center">{{ titulo }}</h1>
    {{ form_start(form) }}
        {% for field in form %}
            <div class="form-group">
                {{ form_row(field, {'attr': {'class': 'form-control', 'placeholder': field.vars.label}}) }}
                {% if form_errors(field) %}
                    <div class="invalid-feedback">
                        {{ form_errors(field) }}
                    </div>
                {% endif %}
            </div>
        {% endfor %}

        <button type="submit" class="boton btn my-3">Guardar</button>
    {{ form_end(form) }}


{# <div >
    <div id="container">
        <h1 class="text-center">{{ titulo }}</h1>
        {{ form_start(form) }}
        
        <div class="table-responsive">
            <table class="table">
                <tbody>
                    <tr>
                        {% for field in form %}
                        <td>
                            <div class="form-group">
                                {{ form_row(field, {'attr': {'class': 'form-control', 'placeholder': field.vars.label}}) }}
                                {% if form_errors(field) %}
                                <div class="invalid-feedback">
                                    {{ form_errors(field) }}
                                </div>
                                {% endif %}
                            </div>
                        </td>
                        {% endfor %}
                    </tr>
                </tbody>
            </table>
        </div>
        
        <button type="submit" class="btn btn-primary my-3">Guardar</button>
        {{ form_end(form) }}
    </div>
</div> #}
